const Grocery = () => {
  return (
    <h1>
      This is a Grocery Page, which will have lots of components inside it and
      it is done by using Lazy Loading.
    </h1>
  );
};

export default Grocery;
